import { Typography } from "@mui/material";
import Button from "@mui/material/Button";

export default function LoginPage() {
  return (
    <div>
      <Typography variant="h1">Login</Typography>
      <Button variant="contained">Hello world</Button>; login
    </div>
  );
}
